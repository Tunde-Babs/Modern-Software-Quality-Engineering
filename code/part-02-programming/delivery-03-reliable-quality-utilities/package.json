{
  "name": "msqe-delivery-03-reliable-quality-utilities",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Companion TypeScript examples for MSQE Part II, Delivery 3.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc --noEmit false --outDir .build",
    "check": "tsc --noEmit",
    "start": "npm run build && node .build/runIllustrativeExample.js",
    "validate": "npm run build && node .build/runValidationScenarios.js"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.9.3"
  }
}
