{
  "name": "msqe-delivery-02-reusable-quality-utilities",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Companion TypeScript examples for MSQE Part II, Delivery 2.",
  "scripts": {
    "build": "tsc --noEmit false --outDir .build",
    "check": "tsc --noEmit",
    "start": "npm run build && node .build/runIllustrativeExample.js",
    "start:environment": "npm run build && node .build/runFromProcessEnvironment.js"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.9.3"
  }
}
