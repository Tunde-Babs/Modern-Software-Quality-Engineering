---
import { status } from '../lib/status';
---
<aside class="readiness" aria-label="Publication readiness" data-pagefind-ignore>
  <p class="eyebrow">{status.version} · {status.designation}</p>
  <p>{status.description}</p>
</aside>
